Do exactly this, nothing else.
1. Run this bash command and print its output verbatim:
   {{cli}} self > {{evidence}}/self.out 2> {{evidence}}/self.err ; echo "SELF_EXIT=$?" > {{evidence}}/self.exit ; cat {{evidence}}/self.exit
2. Print DONE.
Do not ask questions.
